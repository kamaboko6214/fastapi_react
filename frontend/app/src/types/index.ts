export type LoginParams = {
    email: string,
    password: string,
}

export type StockList = {
    name: string,
    genre: string,
    deadline: string,
    count: number,
}

type params = {
    genre_id: number,
    name: string,
    deadline: string
  }
