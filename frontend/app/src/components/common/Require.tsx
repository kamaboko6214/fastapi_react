import { FC, ButtonHTMLAttributes } from "react";

const Require = () => {
  return (
    <label className="mr-4 p-1 rounded-md bg-teal-600 text-white font-bold">必須</label>
  );
};

export default Require
